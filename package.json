{
  "name": "cloudlog",
  "version": "0.1.5",
  "description": "Use your own local browser console to debug the weirdest browser in the most remote locations.",
  "homepage": "https://www.cloudlog.dev",
  "main": "index.js",
  "repository": "https://github.com/cloudlogdev/sdk",
  "author": "Klaus Breyer <klaus@cloudlog.dev> (https://www.cloudlog.dev)",
  "engines": {
    "node": "*"
  },
  "license": "MIT",
  "keywords": [
    "console",
    "console.log",
    "debugging",
    "cloud",
    "developertool",
    "tooling",
    "frontendtooling"
  ],
  "browserslist": [
    "defaults"
  ],
  "scripts": {
    "start": "parcel browser.js --port 2005",
    "build": "rm -rf ./dist && yarn parcel build index.js && yarn parcel build browser.js && echo 'sdk.cloudlog.dev' > ./dist/CNAME"
  },
  "dependencies": {
    "aws-iot-device-sdk": "^2.2.3",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
