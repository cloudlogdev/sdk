{
  "name": "cloudlog",
  "version": "0.1.0",
  "description": "https://www.cloudlog.dev SDK",
  "main": "index.js",
  "repository": "https://github.com/cloudlogdev/sdk",
  "author": "Klaus Breyer <klaus@cloudlog.dev> (https://www.cloudlog.dev)",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.js --port 2005",
    "build": "rm -rf ./dist && yarn parcel build index.js &&  echo 'sdk.cloudlog.dev' > ./dist/CNAME"
  },
  "dependencies": {
    "aws-iot-device-sdk": "^2.2.3",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "parcel": "^1.12.4"
  }
}
